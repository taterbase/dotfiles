[color]
	ui = true
[user]
	name = taterbase
	email = shankga@gmail.com

[github]
  user = taterbase

[push]
	default = simple

[branch "master"]
		rebase = true
[branch]
		autosetuprebase = always
[alias]
	up = "!git remote update -p; git merge --ff-only @{u}"
